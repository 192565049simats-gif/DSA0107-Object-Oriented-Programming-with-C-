#include <iostream>
using namespace std;

int main() {
    bool x = true, y = false;

    cout << (x && y) << endl;
    cout << (x || y) << endl;
    cout << (!x) << endl;
    cout << (!y) << endl;

    return 0;
}
